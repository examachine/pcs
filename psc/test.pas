program test(input, output);

{ well, this is a test program which does few ... }

var
   a, b	: integer;
   c,d	: real;


begin

   b:=3;
   c:=b*b/2;
   a:=0;
   while a<=b do
   begin
      a:=a+1;
      c:=c-0.2;
      if c<3 then
	 a:= a xor b > 5+3 and not +(2.5=c)
      else
      begin
	 a:=a+2;
	 b:=b mod a
      end
   end
end.

